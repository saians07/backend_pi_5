services:
  backend_pi_5:
    container_name: backend_pi5_green
    networks:
      - unified_network
    restart: unless-stopped
    image: ghcr.io/saians07/backend_pi5:latest
    ports:
      - 8083:8080
    environment:
      - PORT=8080

networks:
  unified_network:
    external: true